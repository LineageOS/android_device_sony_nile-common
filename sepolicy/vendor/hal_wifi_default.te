# Allow hal_wifi_default to read to debugfs_wlan
allow hal_wifi_default debugfs_wlan:dir r_dir_perms;

# Allow hal_wifi_default to write to sysfs_wifi
allow hal_wifi_default sysfs_wifi:file w_file_perms;
